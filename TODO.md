# This is the todo after the whole installtion

* Install rust using below command
``` bash
curl https://sh.rustup.rs -sSf | sh
```

* Install stylua for the formatting purpose
```bash
cargo install stylua
```
